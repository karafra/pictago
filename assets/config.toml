[grpc]
port = 9090
host = "localhost"

[http]
port = 8080
host = "localhost"

[database]
port = 5432
host = "localhost"
username = "!secret"
password = "!secret"
migrations_dir = "migrations"
driver = "postgres"
